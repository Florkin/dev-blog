<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
        <a class="navbar-brand" href="{{ url.base_url }}">Dev Blog</a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#">A propos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url.posts_list }}">Les articles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ url.contact }}">Contact</a>
                </li>
                {% if isLogged %}
                    <li class="nav-item">
                        <a href="{{ admin_url.base_url }}/utilisateurs/{{ user_id }}">{{ username }}</a>
                    </li>
                    <li class="nav-item">
                        <a href={{ url.logout }}>Logout</a>
                    </li>
                    <li class="nav-item">
                        <a href={{ admin_url.base_url }}>Admin</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="#" class="nav-link" data-toggle="modal"
                                data-target="#login-modal">Login
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

{% if not isLogged %}
    {% include "_partials/login.twig" %}
{% endif %}




