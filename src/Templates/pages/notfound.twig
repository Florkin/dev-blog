{% extends 'layout.twig' %}

{% block head %}
    <title>Inscription</title>
{% endblock %}

{% block content %}

    {% set herotitle = "Vous êtes perdus?" %}
    {% set herosubtitle = "Cette page n'existe pas" %}
    {% set imgurl = url.base_url ~ '/img/notfound-bg.jpg' %}
    {% include '_partials/hero-section.twig' %}

        <div class="container">
            <div class="row justify-content-center" data-aos="fade-up">
                <div class="col-lg-6 text-center heading-section mb-5">
                    <a class="btn btn-outline-dark" href="{{ url.base_url }}">Retour a l'accueil</a>
                </div>
            </div>
        </div>


{% endblock %}